# CS499_JimCrowLaws

We are using LLamaCloud services which is done with python. Here are the necessary libraries to install

pip3 install llama-parse
pip3 install llama-index
pip3 install dotenv

In the .env file insert your own api then run the jim_crow_ocr.py and select a file to parse.
